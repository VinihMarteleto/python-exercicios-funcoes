# 🐍 Curso de Python - Hashtag Programação

Este repositório reúne meus estudos e práticas do curso de **Python** realizados com as aulas do **Hashtag Programação**.  
O objetivo é consolidar os fundamentos da linguagem e criar uma base sólida para projetos futuros.

---

## 📚 Conteúdos já estudados

### 1. Strings
- Definição e manipulação de textos
- Indexação e fatiamento (`texto[0:5]`)
- Métodos principais:
  - `len()` → tamanho da string
  - `upper()` / `lower()` → maiúsculas e minúsculas
  - `strip()` → remover espaços extras
  - `replace()` → substituir partes do texto
  - `split()` → dividir em lista
- Concatenação de strings com `+`

### 2. Input
- Uso da função `input()` para receber dados do usuário
- Conversão de tipos:
  - `int()` → números inteiros
  - `float()` → números decimais
  - `str()` → texto
- Diferença entre entrada de texto e números

### 3. Print
- Uso da função `print()` para exibir informações
- Exibição de múltiplos valores com vírgula
- F-strings para formatar mensagens:
  ```python
  nome = "Vinih"
  idade = 20
  print(f"Olá, meu nome é {nome} e tenho {idade} anos.")

  
